{
  "name": "lessonforge-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "LessonForge monorepo root — repo-level dev tooling (husky, lint-staged). The Angular app lives in web/; the Python backend in app/.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.8.1"
  },
  "lint-staged": {
    "web/**/*.{ts,html,css,json,md}": "prettier --write --ignore-unknown"
  }
}
